#????????????????????????????????????
#????????????????????????????????????

library(queuecomputer)
library(ggplot2)
library(dplyr)

set.seed(1)
n <- 1000
arrivals_1 <- c(100 + cumsum(rexp(n)), 200 + cumsum(rexp(n)))
#arrivals_1
service_1 <- rexp(2*n, 1/2.5)
#service_1
queue_1 <- queue_step(arrivals = arrivals_1, service = service_1, servers = 20)
#queue_1
walktimes <- rexp(2*n, 1/100)
#walktimes